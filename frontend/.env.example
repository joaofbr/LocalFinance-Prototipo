# Base URL of the LocalFinance REST API.
# Must match the URL the API is listening on (see backend Properties/launchSettings.json).
# On a mobile device/emulator this must point to a host reachable over the
# network (not "localhost"), e.g. the dev machine's LAN IP.
VITE_API_URL=http://localhost:5063/api
